{
  "name": "rs-x-angular-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build:libs": "pnpm -r --filter '!@rs-x/angular' run build",
    "build:angular": "pnpm --prefix rs-x-angular run build",
    "build": "pnpm run build:libs && pnpm run build:angular",
    "test": "vitest run --config projects/rsx/vitest.config.ts",
    "lint": "ng lint"
  },
  "devDependencies": {
    "rimraf": "^6.1.2"
  }
}